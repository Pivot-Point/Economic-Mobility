//Define user information. Create function that pulls information from the USER form. EventListeners will need to be attached to each of the radio buttons, one for race and gender identity. 

//Original state (i.e. health stats, wealth stats, and umobility stats) will need to be represented on screen already. 

//Once user selects race and gender identity state will be manipulated on screen. 

//grab user-information from local storage. 

//The user-information must be parsed from JSON. 

//The state for the first Question will be pulled froNm original state. 
  //

// Questions are assigned an id that correlates with the id in our life event objects.

//Once the 
