//button function
    const ethnicityutton = document.getElementById('ethnicityButton');

    ethnicityButton.addEventListener('click', () => {
        if (FormData === 'Black') {
            //update character Object 
        }
        else if  (FormData === 'White') {
            //update character Object 
        }
        else if(FormData === 'Hispanic') {
            //update character Object 
        }
        else if(FormData === 'Asian') {
            //update character Object 
        }
        else if(FormData === 'Native American') {
            //update character Object 
        }
        else if(FormData === 'Native Hawaiin') {
            //update character Object 
        }
        else (FormData === 'Two or more') {
            //update character Object 
        }

        genderGen(genders) {
    
        };
    })



//continue to life events button function

const continueButton = document.getElementById('continueButton');

continueButton.addEventListener('click', () => {
    genQuestion (arr[0]) {
        //not yet a function
    }
})
